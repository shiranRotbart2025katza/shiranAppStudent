<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShiranApp"
        tools:targetApi="31">

        <activity
            android:name=".OdotActivity"
            android:exported="false" />
        <activity
            android:name=".AnotherQuoteActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- TimerPage Activity definition -->
        <activity
            android:name=".TimerActivity"
            android:exported="true" />
        <activity
            android:name=".CalculatorActivity"
            android:exported="true" />
        <activity
            android:name=".NewDriverActivity"
            android:exported="true" />
        <activity android:name=".JokesActivity">
            <!-- אפשר להוסיף תיאור של ה-Activity אם רוצים -->
        </activity>
        <activity
            android:name=".MotivationActivity"
            android:exported="true" />
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />
        <activity android:name=".ExplnationActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>